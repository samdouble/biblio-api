name: Commitlint

on: [pull_request]

jobs:
  commitlint:
     name: Commitlint
     runs-on: ubuntu-latest
     continue-on-error: true
     steps:
      - name: Commitlint
        uses: wagoid/commitlint-github-action@v6
